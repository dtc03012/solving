#include <iostream>
#include <map>
#include <set>
#include <stdio.h>
#include <vector>
using namespace std;
struct inf{
	int x,y,val;
};
inf arr[111111];
map<int,set<int> > xx;
map<int,set<int> > yy;
int checking(int x,int y,int val)
{
	int type=-1,ma=-1;
	//1;
	int f=0;
	if(xx[y].size()!=0)
	{
		if(*xx[y].begin()<x)
		
	}
}
int main(void)
{
	int n,tot=0;
	scanf("%d",&n);
	for(int e=0;e<n;e++)
	{
		int x,y,val;
		scanf("%d%d%d",&x,&y,&val);
		
	}
}
